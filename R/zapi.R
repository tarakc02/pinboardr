posts <- list(
    add = add_post,
    delete = delete_post,
    update = last_updated,
    all = all_posts,
    dates = post_dates,
    get = get_posts,
    suggest = suggest
)

tags <- list(
    get = get_tags
)
